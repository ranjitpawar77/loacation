import { LogitudeDirective } from './logitude.directive';

describe('LogitudeDirective', () => {
  it('should create an instance', () => {
    const directive = new LogitudeDirective();
    expect(directive).toBeTruthy();
  });
});
